<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:scrollbars="vertical" >

    <!-- P96, 97 -->

    <ImageView
        android:id="@+id/student_share_item_imageView"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/student_share_button_linearlayout"
        android:layout_below="@+id/student_share_item_textview"
        android:contentDescription="@string/student_dialog_share_item_imageView"
        android:src="@drawable/ic_launcher" />

    <LinearLayout
        android:id="@+id/student_share_button_linearlayout"
        style="?android:attr/buttonBarStyle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:background="#3E80B4"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/student_share_item_btn_yes"
            style="?android:attr/buttonBarStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#003366"
            android:clickable="true"
            android:text="@string/yes"
            android:textColor="#ffffff"
            android:textStyle="bold" />

        <Button
            android:id="@+id/student_share_item_btn_no"
            style="?android:attr/buttonBarStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#003366"
            android:clickable="true"
            android:text="@string/no"
            android:textColor="#ffffff"
            android:textStyle="bold" />
    </LinearLayout>

    <TextView
        android:id="@+id/student_share_item_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="@string/student_dialog_share_item"
        android:textStyle="bold" />

</RelativeLayout>